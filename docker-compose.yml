version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - ENVIRONMENT=development
    env_file:
      - .env
    command: uvicorn src.main:app --host 0.0.0.0 --port 5000 --reload
